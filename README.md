# Deals & Coupons Finder — Day 1 Base Services

This package contains the requested Day 1 source files for:

- auth-service
- customer-service

Included:
- Auth `User` entity
- Auth `Role` enum
- `AuthController`
- Customer `Customer` entity
- Customer `CustomerStatus` enum
- `CustomerController`

The controllers reference DTO/service classes from the larger project, so this ZIP is intended as the requested GitHub source base. Add the remaining DTO, service, repository, configuration, and build files as they are implemented.
